{
  "name": "@borodutch-tools/backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4",
    "@types/pg": "^8.15.6",
    "@types/ws": "^8.18.1",
    "bs58": "^6.0.0",
    "pg": "^8.16.3",
    "tweetnacl": "^1.0.3",
    "viem": "^2.39.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "typescript": "~6.0.2",
    "vitest": "^3.2.4"
  }
}
